image:
  file: .gitpod.Dockerfile

tasks:
  - init: sdk use java 19-open
    command: cd FridgeManager
    command: cd springboot;mvn spring-boot:run
    command: cd ..;mvn claen install


ports:
  # used by virtual desktop and vnc, supports JavaFX
  - port: 6080
