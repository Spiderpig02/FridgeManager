image:
  file: .gitpod.Dockerfile

tasks:
  - init: sdk use java 19-open
    command: cd FridgeManager;cd springboot;mvn spring-boot:run;cd ..;mvn clean install


ports:
  # used by virtual desktop and vnc, supports JavaFX
  - port: 6080
